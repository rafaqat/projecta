rules:
  - id: no-dynamic-code-execution
    message: Dynamic code execution is forbidden; it defeats the output-handling controls (ADR-028)
    severity: ERROR
    languages: [javascript, typescript]
    patterns:
      - pattern-either:
          - pattern: eval(...)
          - pattern: new Function(...)
    paths:
      exclude:
        - node_modules
        - build
