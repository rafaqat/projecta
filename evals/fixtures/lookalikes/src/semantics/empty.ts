/** True twins below any sensible duplicate-size floor. */
export function isEmptyA(items: unknown[]): boolean {
  return items.length === 0
}

export function isEmptyB(items: unknown[]): boolean {
  return items.length === 0
}
